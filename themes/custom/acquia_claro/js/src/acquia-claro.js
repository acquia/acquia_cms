/**
 * @file
 * Acquia claro behaviors.
 */

(function ($, Drupal) {

  'use strict';

  /**
   * Behavior description.
   */
  Drupal.behaviors.acquiaClaro = {
    attach: function (context, settings) {
      // Example behavior.
    }
  };

} (jQuery, Drupal));

